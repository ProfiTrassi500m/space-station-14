public sealed class WeaponAttachmentSystem : EntitySystem
{
    public override void Initialize()
    {
        SubscribeLocalEvent<GunComponent, InteractUsingEvent>(OnInteractUsing);
    }

    private void OnInteractUsing(EntityUid uid, GunComponent gun, InteractUsingEvent args)
    {
        // Логика присоединения модулей
    }
}